module Main
where

import Prelude
import Parser
import JSON
import System.Environment

-- | Prompt user for a JSON file, parse it and pretty print the result.
--
-- /Tip:/ use @getArgs@, @readJsonFile@ and @print@
--
-- You can run this function in GHCi by calling:
-- > :main "input.json"
main :: IO ()
main = undefined
